import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF31C5F4);
  static const Color background = Color(0xFFFFFFFF);

  // Text colors
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF656565);

  // Status colors
  static const Color lightGreen = Color(0xFFD3FFD0);
  static const Color darkGreen = Color(0xFF0BB908);
  static const Color lightRed = Color(0xFFFFD0D1);
  static const Color darkRed = Color(0xFFFF6366);
  static const Color lightYellow = Color(0xFFFFDB7F);
  static const Color darkBrown = Color(0xFFB57F00);

  // Utility colors
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color grey = Color(0xFF9E9E9E);
  static const Color lightGrey = Color(0xFFF5F5F5);

  // Success states
  static const Color success = darkGreen;
  static const Color successLight = lightGreen;

  // Error states
  static const Color error = darkRed;
  static const Color errorLight = lightRed;

  // Warning states
  static const Color warning = darkBrown;
  static const Color warningLight = lightYellow;
}
